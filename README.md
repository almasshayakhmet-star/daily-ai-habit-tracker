# Толық сабақ конструкторы

![Сабақ конструкторы үлгісі](https://github.com/user-attachments/assets/3b086b87-c026-4fa9-be5b-047196b4bad5)

**Пәні:** Информатика  
**Сынып:** 7-сынып  
**Тақырыбы:** Python тіліндегі шартты операторлар (`if`, `elif`, `else`)  
**Ұзақтығы:** 45 минут

## 1. Негізгі құзырет
- Алгоритмдік ойлау арқылы қарапайым есептерді шартпен шешу.
- Python-да шешім қабылдау құрылымын қолдану.

## 2. Оқу нәтижесі
Оқушы:
- `if / elif / else` синтаксисін біледі;
- шартты өрнектерді (`>`, `<`, `==`, `!=`) қолданады;
- қарапайым бағдарламалар жазады және нәтижесін түсіндіреді.

## 3. Оқу мақсаты
- **7.3.1.1** Python-да шартты операторларды қолдану;
- **7.3.1.2** Енгізу/шығару арқылы есепті бағдарламалау.

## 4. Сабақ мақсаты
- Шартты оператор синтаксисін дұрыс жазады;
- Бір және көп тармақты шарт құрады;
- Қарапайым практикалық есептерді кодпен шешеді.

## 5. Тапсырмалар (деңгейлік)

### 1-деңгей (бастапқы)
**Тапсырма:** Пайдаланушы енгізген санның жұп не тақ екенін шығару.  
**Күтілетін әрекет:** `if` арқылы тексеру.

```python
n = int(input("Сан енгізіңіз: "))

if n % 2 == 0:
    print("Жұп сан")
else:
    print("Тақ сан")
```

### 2-деңгей (орташа)
**Тапсырма:** Оқушының ұпайын енгізіп, баға шығару:  
90–100 → “5”, 70–89 → “4”, 50–69 → “3”, 0–49 → “2”.  
**Күтілетін әрекет:** `if-elif-else` тізбегі.

```python
score = int(input("Ұпайды енгізіңіз (0-100): "))

if 90 <= score <= 100:
    print("Баға: 5")
elif 70 <= score <= 89:
    print("Баға: 4")
elif 50 <= score <= 69:
    print("Баға: 3")
elif 0 <= score <= 49:
    print("Баға: 2")
else:
    print("Қате: ұпай 0-100 аралығында болуы керек")
```

### 3-деңгей (жоғары)
**Тапсырма:** Екі сан және амал таңбасы (`+`, `-`, `*`, `/`) беріледі. Калькулятор нәтижесін шығару.  
**Қосымша шарт:** бөлу кезінде 0-ге бөлуді өңдеу.

```python
a = float(input("Бірінші сан: "))
b = float(input("Екінші сан: "))
op = input("Амал (+, -, *, /): ")

if op == "+":
    print("Нәтиже:", a + b)
elif op == "-":
    print("Нәтиже:", a - b)
elif op == "*":
    print("Нәтиже:", a * b)
elif op == "/":
    if b == 0:
        print("Қате: 0-ге бөлуге болмайды")
    else:
        print("Нәтиже:", a / b)
else:
    print("Қате: амал таңбасы дұрыс емес")
```

## 6. Бағалау критерийі
- Шартты операторды дұрыс жазады;
- Логикалық шартты дұрыс құрады;
- Бағдарлама нәтижесін дұрыс шығарады;
- Қате жағдайларды (мысалы, 0-ге бөлу) ескереді.

## 7. Дескриптор (оқушы)
- `if` құрылымын дұрыс қолданады;
- `elif/else` тармақтарын орынды жазады;
- Енгізілген мәнге сай дұрыс жауап шығарады;
- Кодта синтаксистік қате жібермейді.

## 8. Сабақ барысы (45 минут)
- **0–5 мин:** Ұйымдастыру, өткенді еске түсіру.
- **5–12 мин:** Жаңа тақырып түсіндіру (шартты оператор).
- **12–20 мин:** Мұғаліммен бірге 1 мысал талдау.
- **20–35 мин:** Деңгейлік практикалық тапсырмалар.
- **35–41 мин:** Жұппен тексеру, талқылау.
- **41–45 мин:** Рефлексия, үй тапсырмасы.

## 9. Рефлексия (БББ)
- **Білдім:** бүгін не үйрендім?
- **Білгім келеді:** қандай сұрағым қалды?
- **Қиын болды:** қай жері түсініксіз болды?

## 10. Қолданылатын әдіс-тәсілдер
- Түсіндіру;
- Көрсету;
- Практикалық жұмыс;
- Жұптық тексеру;
- Кері байланыс.

## 11. Қолданылатын құралдар
- Компьютер;
- Python ортасы (Thonny/IDLE);
- Интерактивті тақта;
- Тапсырма парағы.

## 12. Үй тапсырмасы
- «Енгізілген санның оң/теріс/нөл екенін анықтайтын» бағдарлама жазу.
- Қосымша: үш санның ең үлкенін табу.
